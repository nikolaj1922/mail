export interface IEmail {
  to: string;
  subject: string;
  message: string;
  timestamp: string;
}

export interface IEmailRow {
  id: string;
  title: string;
  subject: string;
  description: string;
  time: string;
}
